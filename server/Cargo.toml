[package]
name = "server"
version = "0.1.0"
edition = "2021"

[workspace]
members = [".", "api", "entity", "migration"]

[dependencies]
api = { path = "api" }
dotenv = "0.15.0"
env_logger = "0.9.0"
log = "0.4.17"
